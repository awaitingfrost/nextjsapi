const Home = () => {
  return (
    <div className="card">
      <h2 className="">This is Homepage</h2>
      <br />
      <p>
        A home page is actually a webpage that can serve as the starting point.
        It's the default web page that loads if you enter a site's domain only.
        The home page is actually utilized to facilitate navigation to various
        other web pages on the website by providing links to all the areas
        available on the site.
      </p>
    </div>
  );
};

export default Home;
